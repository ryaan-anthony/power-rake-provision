defaults: &defaults
  project: <%= ENV.fetch('RAKE_PROJECT') %>
  backend:
    bucket: <%= ENV.fetch('AWS_S3_BUCKET') %>
    lock_table: <%= ENV.fetch('AWS_DYNAMODB_TABLE') %>

development:
  <<: *defaults

staging:
  <<: *defaults

production:
  <<: *defaults